# Pin x86_64 builds to x86-64-v3 (Haswell+: AVX2, FMA, BMI1/2, LZCNT, MOVBE)
# This prevents SIGILL on CI runners with varying CPU microarchitectures,
# while still enabling modern SIMD for good performance.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "target-cpu=x86-64-v3"]
